server {
    listen {{ nginx_port }};
    server_name localhost;

    location / {
        return 200 'Nginx is installed and configured via Ansible!';
        add_header Content-Type text/plain;
    }
}
